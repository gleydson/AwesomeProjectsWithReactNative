import React, { Component } from 'react';
import { Text } from 'react-native';

export default class SceneAboutGame extends Component {
     render() {
          return (
               <Text style={{ flex: 1 }}>
                    Aqui pode ser apresentado informações sobre o jogo.
               </Text>
          );
     }
}
